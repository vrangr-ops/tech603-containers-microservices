# Step 1: Use the official Nginx base image from Docker Hub
FROM nginx:latest

# Step 2: Copy your local index.html file to the location of the Nginx default page in the container
COPY index.html /usr/share/nginx/html/index.html
